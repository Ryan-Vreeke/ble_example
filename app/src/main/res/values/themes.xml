<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Ble_example" parent="Theme.Material3.DayNight.NoActionBar">
            <!-- res/values/themes.xml -->
            <!-- Customize your theme here. -->
        <item name="colorPrimary" type="color">@color/purple_500</item>
        <item name="colorPrimaryVariant" type="color">@color/purple_700</item>
        <item name="colorOnPrimary" type="color">@color/white</item>
        <!-- Ensure this color is set to control icon color dynamically -->
        <item name="colorControlNormal" type="color">@color/iconColor</item>
    </style>
</resources>

